#include <UniversalUI/Angelo/aRenderer.h>

//  generate a new pixel buffer in GPU memory with a given size
aPixelBuffer* aRenderer::NewPixelBuffer(uSize size) {

}

//  resize an existing pixel buffer
void aRenderer::ResizePixelBuffer(aPixelBuffer* buffer, uSize size) {

}

//  fill a buffer with a given colour - use mainly for testing purposes
void aRenderer::ClearBuffer(aPixelBuffer* buffer, uColour colour) {
    
}
